## 1. 重新生成逻辑

- [ ] 1.1 在 App 组件中新增 `regenerateMessage` 函数：截断到最后一条用户消息作为上下文，清空对应 AI 消息，重新调用 `/api/chat`
- [ ] 1.2 `regenerateMessage` 复用现有 `sendMessage` 的 SSE 流式接收逻辑，更新替换的 AI 消息内容
- [ ] 1.3 重新生成期间设置 `loading=true`，完成后恢复 `loading=false`

## 2. UI 按钮

- [ ] 2.1 在 `MessageBubble` 中为 AI 消息添加重新生成按钮（🔄 图标），仅在 hover 时显示
- [ ] 2.2 按钮通过 props 接收 `onRegenerate` 回调，点击时调用
- [ ] 2.3 按钮定位在 AI 消息气泡右下角，样式与整体设计一致

## 3. 状态保护

- [ ] 3.1 重新生成进行中时，隐藏重新生成按钮，避免重复触发
- [ ] 3.2 重新生成进行中时，禁用输入框和发送按钮（复用现有 loading 逻辑）
- [ ] 3.3 确保重新生成不影响现有的消息复制功能

## 4. 验证

- [ ] 4.1 验证点击重新生成按钮后 AI 回复被替换为新内容
- [ ] 4.2 验证重新生成期间显示 loading 状态
- [ ] 4.3 验证重新生成后对话上下文正确（新回复基于之前全部历史）
- [ ] 4.4 验证刷新页面后重新生成的消息被正确持久化
- [ ] 4.5 验证消息复制功能仍正常工作
