## 1. 点击复制逻辑

- [x] 1.1 在 `MessageBubble` 组件中添加 `copied` 状态管理
- [x] 1.2 为消息气泡外层添加 `onClick` 处理器，调用 `copyToClipboard(msg.content)` 并设置 `copied` 状态
- [x] 1.3 确保 AI 消息点击复制时使用 `msg.content` 纯文本，而非 HTML 渲染结果

## 2. 视觉反馈

- [x] 2.1 为消息气泡添加 `cursor: pointer` 样式，暗示可点击
- [x] 2.2 添加复制成功后的 toast 反馈元素（显示"已复制"文字，1.5 秒后自动消失）

## 3. 验证

- [x] 3.1 验证点击用户消息可复制内容并显示"已复制"反馈
- [x] 3.2 验证点击 AI 消息可复制纯文本内容并显示"已复制"反馈
- [x] 3.3 验证复制反馈 1.5 秒后自动消失
