## 1. 时钟功能

- [x] 1.1 新增 `formatTime` 函数，使用 `Intl.DateTimeFormat` 格式化时间为 HH:MM:SS
- [x] 1.2 在 DateDisplay 组件中添加 clock 状态，每秒更新
- [x] 1.3 将 `setInterval` 间隔从 60 秒改为 1 秒，同时驱动日期和时钟更新

## 2. 样式调整

- [x] 2.1 在日期下方添加时钟文本，字体略小（13px）
- [x] 2.2 保持右上角固定定位和主题颜色一致

## 3. 验证

- [x] 3.1 验证时钟格式为 HH:MM:SS
- [x] 3.2 验证时钟每秒更新
- [x] 3.3 验证对话框打开时不影响时钟显示
