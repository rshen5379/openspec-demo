schema: spec-driven

# 项目上下文（强烈建议填写）
# OpenSpec 技能创建 proposal 时依赖此上下文来理解项目。
# 不填写时 AI 会从代码推断，效果不稳定。
# TODO: 替换为你的项目信息
context: |
  Tech stack: React 18 + Vite 5 (frontend), Express 4 + OpenAI SDK (backend)
  Language: JavaScript (ES2022+)
  Frontend: Single-file SPA in App.jsx, inline styles, localStorage persistence
  Backend: SSE streaming via POST /api/chat
  Styling: No CSS framework, theme system via themes.js/useTheme.js
  Conventions: All code changes must follow OpenSpec workflow

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
